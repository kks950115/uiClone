<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Uiclone" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Uiclone" parent="Base.Theme.Uiclone" />
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- BottomNavigationView에서 선택 효과 제거 -->
        <item name="android:itemBackground">@android:color/transparent</item>
    </style>
</resources>